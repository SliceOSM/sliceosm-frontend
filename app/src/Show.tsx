import React from 'react'
import ReactDOM from 'react-dom/client'
import ShowComponent from './ShowComponent.tsx'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <ShowComponent />
  </React.StrictMode>,
)
